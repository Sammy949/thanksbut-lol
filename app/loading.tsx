import { ArchiveLoading } from "@/components/archive/archive-loading";

/** Shown while the page streams (used once data fetching is async). */
export default function Loading() {
  return <ArchiveLoading />;
}
