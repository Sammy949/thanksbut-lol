"use client";

import { Button } from "@/components/ui/button";

/**
 * Drawer for archiving a rejection. Placeholder — the real flow (screenshot
 * upload via UploadThing, category, optional company / caption / display name,
 * validated with react-hook-form + zod) is built later.
 */
export function UploadDrawer() {
  return (
    <div>
      <Button>Archive Yours</Button>
    </div>
  );
}
